# Environment: test

Root Terraform configuration for the test environment. Composes the shared modules under
`../../modules/` with test-specific variables and its own state backend — mirrors `../dev` once
that environment's modules are proven out.

Not yet implemented.

# Scripts

Thin operational wrappers for this project.

- `fab-deploy.ps1` — deploys `../fabric-items/` content onto the Fabric item shells created by
  Terraform, using the project venv's Fabric CLI (`.venv\Scripts\fab.exe`). Not yet written.
